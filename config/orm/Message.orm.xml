<?xml version="1.0" encoding="UTF-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                                      http://doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <!-- The host's entity names the table and maps `conversation` to its own conversation class. -->
    <mapped-superclass name="Odiseo\AiConversationalAgentBundle\Bridge\Doctrine\Model\Message">
        <id name="id" column="id" type="integer">
            <generator strategy="AUTO" />
        </id>

        <field name="position" column="position" type="integer" />
        <field name="role" column="role" type="string" length="16" />
        <field name="text" column="text" type="text" nullable="true" />
        <field name="payload" column="payload" type="json" />
        <field name="createdAt" column="created_at" type="datetime_immutable" />
    </mapped-superclass>
</doctrine-mapping>
